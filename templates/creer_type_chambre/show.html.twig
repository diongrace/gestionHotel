{% extends 'base.html.twig' %}

{% block title %}CreerTypeChambre{% endblock %}

{% block body %}
    <h1>CreerTypeChambre</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ creer_type_chambre.id }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ creer_type_chambre.libelle }}</td>
            </tr>
            <tr>
                <th>Rang</th>
                <td>{{ creer_type_chambre.rang }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_creer_type_chambre_index') }}">back to list</a>

    <a href="{{ path('app_creer_type_chambre_edit', {'id': creer_type_chambre.id}) }}">edit</a>

    {{ include('creer_type_chambre/_delete_form.html.twig') }}
{% endblock %}
