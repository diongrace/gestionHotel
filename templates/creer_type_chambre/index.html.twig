{% extends 'base.html.twig' %}

{% block title %}CreerTypeChambre index{% endblock %}

{% block body %}
    <h1>CreerTypeChambre index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Rang</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for creer_type_chambre in creer_type_chambres %}
            <tr>
                <td>{{ creer_type_chambre.id }}</td>
                <td>{{ creer_type_chambre.libelle }}</td>
                <td>{{ creer_type_chambre.rang }}</td>
                <td>
                    <a href="{{ path('app_creer_type_chambre_show', {'id': creer_type_chambre.id}) }}">show</a>
                    <a href="{{ path('app_creer_type_chambre_edit', {'id': creer_type_chambre.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_creer_type_chambre_new') }}">Create new</a>
{% endblock %}
